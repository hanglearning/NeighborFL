# device init k = 1
# device.neighbors_close_to_far = ['19972', '19968', ...]  # sorted list of its neighbors, prepopulated at the beginning of the program

for cr in range(1, max_rounds + 1):
    
    